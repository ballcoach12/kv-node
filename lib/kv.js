export async function append(key, value) {
    //todo: add the value to the specified key in mongo
}
export function printUsage() {
    console.log('TODO: write a color-coded usage message per the requirements');
}
